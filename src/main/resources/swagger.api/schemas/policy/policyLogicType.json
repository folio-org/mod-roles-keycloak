{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Policy logic enum",
  "description": "The logic type of policy.",
  "type": "string",
  "enum": [
    "positive",
    "negative"
  ],
  "default": "positive",
  "examples": [
    "positive",
    "negative"
  ]
}
